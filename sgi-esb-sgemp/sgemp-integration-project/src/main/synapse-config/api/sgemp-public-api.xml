<?xml version="1.0" encoding="UTF-8"?>
<api context="/sgemp/public" name="sgemp-public-api" xmlns="http://ws.apache.org/ns/synapse">

  <!-- empresas -->
  <resource methods="GET" uri-template="/empresas/{id}">
    <inSequence>
      <property name="url" expression="get-property('axis2','REST_URL_POSTFIX')" />
      <rewrite inProperty="url" outProperty="url">
        <rewriterule>
          <action type="replace" regex="/empresas" value="/public/empresas" fragment="path" />
        </rewriterule>
      </rewrite>
      <property name="REST_URL_POSTFIX" scope="axis2" expression="get-property('url')" />
      <send>
        <endpoint key="sgemp-endpoint" />
      </send>
    </inSequence>
    <outSequence>
      <respond />
    </outSequence>
    <faultSequence>
      <sequence key="error-handling-sequence" />
    </faultSequence>
  </resource>

</api>